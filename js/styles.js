galleryImg.forEach(element => {
  element.classList.add('min-w-full', 'h-64', 'border', 'border-black', 'bg-slate-500', 'rounded-2xl');
});

footerIcons.forEach(element => {
  element.classList.add('h-12', 'rounded-full', 'bg-white', 'p-[1px]');
});

